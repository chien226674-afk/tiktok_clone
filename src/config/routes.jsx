const routes = {
   home: "/",
   following: "/following",
   chat: "/chat",
   profile: "/profile/:nickname",
   videoPage: "/:nickname/video/:videoId",
   uploadPage: "/upload",
   test: "/"
}

export default routes